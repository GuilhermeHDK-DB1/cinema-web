import { ClassificacaoIndicativaPipe } from './classificacao-indicativa.pipe';

describe('ClassificacaoIndicativaPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassificacaoIndicativaPipe();
    expect(pipe).toBeTruthy();
  });
});
